# Decision Log — PRD: Broker-Neutral Trading Execution Library

## 2026-06-17 — Create (bmad-prd, create intent, Fast path)

**Input:** the canonical spec `_bmad-output/specs/spec-broker-neutral-execution/` (SPEC.md CAP-1…CAP-34 + 11 companions + resolved `technical-architecture.md` D1–D8 + staged-product decision). No new elicitation — the spec is rich; Fast path drafts directly with `[ASSUMPTION]` tags.

**Structure decisions**
- Developer-product PRD: pulled in the Developer-Product cluster (API surface, versioning, runtime targets), Cross-Cutting NFRs, Constraints & Guardrails, Why Now, and a Risk register — appropriate for a safety-critical real-money library.
- 34 capabilities mapped to **14 features (§4.1–4.14)** with **37 FRs (FR-1…FR-37)**, globally numbered for stable downstream reference. Each FR's testable consequences inherit the spec's per-capability success criteria.
- 5 named User Journeys (UJ-1…UJ-5); UJ-2 (timeout→UNKNOWN→no duplicate) and UJ-3 (safe-start) are the load-bearing safety moments.
- Glossary adopts the spec glossary verbatim (no synonyms introduced).
- MVP In/Out mirrors the spec's `scope-and-phasing.md` tiers (must-have = In; should-have/later = Out, with a `[NOTE FOR PM]` flag on hedge-first/basket).
- Success metrics anchored on the hard invariant (SM-1 zero duplicate orders) + the production checklist (SM-2) + portability (SM-3), with two counter-metrics guarding against gaming safety/alerts.

**Open questions carried (5):** product positioning/distribution, target segment, proof anchoring, replay data source, static-IP boundary — items 1–3 are the same business calls flagged in the spec (need market input, not analysis); none block architecture.

**Assumptions tagged (indexed §9):** single-account single-process MVP boundary; public-PyPI open-core; replay = own logs; runtime stack from D1–D8; quantitative NFR budgets are starting targets.

**Status:** draft. Not yet run through the Reviewer Gate / Finalize. Next: user review (Fast-path iteration), then optional reviewer gate, then `bmad-create-architecture`.
